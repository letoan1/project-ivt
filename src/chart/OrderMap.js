import React from 'react';
import { VectorMap } from '@south-paw/react-vector-maps';
import { Map } from '../constants/VNMap';

const OrderMap = () => {
    return <VectorMap {...Map} />;
};

export default OrderMap;
